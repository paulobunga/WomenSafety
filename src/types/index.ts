export interface ISender {
  name: string;
  phone: string;
}

export interface ICoordinates {
  latitude: number;
  longitude: number;
}

export interface IChild {
  name: string;
  address: string;
  contact: string;
  image: string;
  createdAt: any;
  age: number;
}
